[project]
name = "agent"
version = "0.1.0"
description = "GitHub chat agent using AgentCore Consent Portal"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bedrock-agentcore>=1.15.1",
    "mcp>=1.28.1",
    "strands-agents>=1.45.0",
]
